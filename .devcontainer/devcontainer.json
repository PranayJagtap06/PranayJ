{
    "name": "My Codespace",
    "customizations": {
      "vscode": {
        "settings": {
          "terminal.integrated.defaultProfile.linux": "bash"
        }
      }
    },
    "postCreateCommand": "chmod +x rename_portfolios && echo 'export PATH=$PATH:$PWD' >> ~/.bashrc && source ~/.bashrc"
  }